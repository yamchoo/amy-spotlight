import { ArticleLayout } from '@/components/ArticleLayout'
import designSystem from './image-2.jpg'

export const article = {
  author: 'Amy Chou',
  date: '2022-07-14',
  title: 'Book Club 4 Life',
  description:
    'What started as a hobby is now my community, learning engine, and networking tool.',
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

I run a book club! I'll write more about it later though. Here's a pic of all of us hanging out.
<Image src={designSystem} alt="" />
